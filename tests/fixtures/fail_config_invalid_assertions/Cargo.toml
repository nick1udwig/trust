[package]
name = "fail_config_invalid_assertions"
version = "0.0.0"
edition = "2021"

[workspace]

[lib]
doctest = false

[dependencies]
trust = { path = "../../../crates/trust" }

[package.metadata.trust]
assertions = "sometimes"
solver = "mock"
