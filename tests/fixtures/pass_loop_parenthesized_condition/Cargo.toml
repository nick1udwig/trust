[package]
name = "pass_loop_parenthesized_condition"
version = "0.0.0"
edition = "2021"

[workspace]

[lib]
doctest = false

[dependencies]
trust = { path = "../../../crates/trust" }

[package.metadata.trust]
assertions = "always"
solver = "mock"
