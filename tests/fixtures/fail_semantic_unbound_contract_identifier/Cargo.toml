[package]
name = "fail_semantic_unbound_contract_identifier"
version = "0.0.0"
edition = "2021"

[workspace]

[lib]
doctest = false

[dependencies]
trust = { path = "../../../crates/trust" }

[package.metadata.trust]
assertions = "always"
solver = "z3"
